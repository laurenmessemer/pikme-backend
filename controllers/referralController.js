const { User } = require("../models");

// You can keep this as a placeholder for future referral-related endpoints if needed
exports.placeholderReferralHandler = (req, res) => {
  res.status(200).json({ message: "Referral system active. Email invite is disabled for now." });
};
